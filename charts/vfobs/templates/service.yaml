apiVersion: v1
kind: Service
metadata:
  name: {{ include "vfobs.fullname" . }}
  labels:
    {{- include "vfobs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vfobs.selectorLabels" . | nindent 4 }}
